"""Model training and hyperparameter optimization.

This subpackage holds the research/training code (model definitions, data
pipeline, trainer, and run entry point). It depends on the heavy
``udonpred[training]`` stack and is not shipped in the CAID inference container.
"""
